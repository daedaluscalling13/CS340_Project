var pool = require('dbcon').pool

const newEntry = async (request, response) => {
    const {

    }
    pool.query('INSERT INTO locations (userID, DOE, timeOfDay, locationID, category, title, comments, review, groupSize ) VALUES ()')
}